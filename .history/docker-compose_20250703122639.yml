services:
  health-orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"  # Maps container port 8000 to your local port
    volumes:
      - .:/app  # Live code mounting (optional in production)
    environment:
      - ENV_VAR_NAME=value  # Optional: put your real ENV vars here
