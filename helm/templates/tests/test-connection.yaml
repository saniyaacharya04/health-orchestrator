apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-health-orchestrator.fullname" . }}-test-connection"
  labels:
    {{- include "helm-health-orchestrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-health-orchestrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
